# Subsystems Overview

Subsystems group the low-level services that recipes rely on: config actions, checkpoints, default content, and the recipes runtime itself. Browse each subsystem before diving into the generated API reference.
