# Config Checkpoints Overview

Configuration checkpoints snapshot the entire configuration state so you can compare, roll back, or audit changes. This overview explains how checkpoints are stored, when they are created, and which services keep their history consistent.
