# Default Content Overview

Default content services allow recipes and install profiles to ship curated content. Learn how exporters, importers, and events coordinate to serialize entities and seed a site during installation.
